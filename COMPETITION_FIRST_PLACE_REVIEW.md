# AI Tutor 竞赛第一名差距审查

> 文档状态：`REVIEW_COMPLETE / PLANNING_ONLY`
>
> 审查日期：2026-08-01
>
> 实施边界：本文件保留原始差距审查；`IMPLEMENTATION_PLAN.md` 仍是阶段权威。P6 与 P7 已于 2026-08-02 获用户批准，当前为 `P8_IN_PROGRESS`；P8、部署、真人研究和后续阶段均已授权，购买路径明确排除。
>
> 执行更新（2026-08-03）：本审查最初记录的盒模型纵向闭环、P6 三主题 Live、文字降级、打断/恢复、能力边界和日志治理缺口，已有机器证据收口；v2 又修复了最终用户转写前 AI 输出竞态，并冻结 20/20 冷暖逐样本证据，见 `P6_MACHINE_ACCEPTANCE_2026-08-01.md`。P7 的版本化事件、PostgreSQL 权威快照、本地 outbox 与交互回放也已机器验收并获用户批准，见 `P7_MACHINE_ACCEPTANCE_2026-08-02.md`。P8 已完成三主题、浏览器/移动端、故障、安全、视觉、自动无障碍、短时性能、发布、供应链和隔离恢复的本机机器候选，并增加事件级误区/自适应、断言事实门禁、可下载审计包，以及 Tutor 文字/合成语音、工具、事实和画布保存动作的统一 Learning Proof Replay。100 项当前为 `75 VERIFIED / 1 IN_PROGRESS / 24 BLOCKED / 0 NOT_STARTED`；只有 083 因用户取消重跑 30 分钟浸泡而保持 `IN_PROGRESS`。统一回放证据见 `docs/P8_LEARNING_PROOF_REPLAY_ACCEPTANCE_2026-08-03.md`；当前数值和边界见 `docs/RESULTS_AND_LIMITATIONS_2026-08-02.md`，提交/视频只规划未执行，见 `docs/SUBMISSION_AND_VIDEO_DEMO_PLAN.md`。原始差距文字作为审查快照保留，不能再当作当前实现状态。总判断仍为 NO-GO，因为官方规则、真人研究、专家/法务、Git 来源、异机现场、盲演和独立 go/no-go 尚未完成。

## 1. 一句话结论

**当前决策：`NO-GO（尚不能以“第一名准备完成”上台）`。**

审查快照中的项目是一套有辨识度、工程底座扎实的“AI 驱动 CSS 可视化实验台”，当时还不是一套能让评委看见并相信“学生真的学会了”的成品级 AI Tutor。2026-08-01 已完成一个可测、可重复、无需麦克风也能开始的盒模型机器闭环；真人学习成效和长期保持仍无证据。

距离第一名最关键的一步，不是继续堆画布块、模型或炫技效果，而是把现有能力收成一条可以在 5–7 分钟内完成、可以测量、可以复核的学习证据链：

```text
诊断学生 → 让学生先预测 → 学生亲手操作/写代码 → 系统识别误区
→ 分级提示 → 学生解释因果 → 完成陌生迁移题 → 形成可追溯的课内达成证据
```

审查时产品主要覆盖中间的“AI 建实验、学生拖动、保存、对比”；当前已补上盒模型的预测、解释、单次即时迁移和课内收据，但第二项陌生迁移、延迟保持和真人证据仍缺失。

## 2. 审查范围与证据边界

### 2.1 本轮实际完成

- 完整阅读 `MVP_SPEC.md`、`ARCHITECTURE.md`、`IMPLEMENTATION_PLAN.md`、`AGENTS.md`、`README.md`、`HANDOFF.md` 与研究报告。
- 检查前端、运行时、Realtime 工具协议、教学模型、数据库 schema、单元测试与 E2E 测试。
- 检查设备本地 Realtime 诊断日志中的成功工具链、延迟、断连与 MCP 启动记录；日志会清理认证字段、SDP 和部分敏感模式，但本轮没有把它认定为完整 PII 脱敏。
- 在全新隔离 Playwright 会话中模拟首次用户，不连接 Chrome 历史画布，不启用物理麦克风，也未播放语音。
- 实际走通：空白首屏 → 新建知识卡 → 导入仓库示例 → 选择页面元素 → 调节里面留白 → 保存版本 → 自动生成前后对比。
- 实测桌面 `1440×900` 与手机 `390×844` 两种视口。

### 2.2 本轮没有声称完成

- 没有进行真人非静音听感验收。
- 没有重新进行账户相关的 Live Realtime 调用；相关判断来自现有测试、实施记录和本机诊断日志。日志已有认证材料/SDP 清理，但不等于完整 PII 脱敏。
- 没有真实学生学习成效数据、专家内容审核或正式可用性研究。
- 用户没有提供具体比赛名称与官方评分表，因此下文只给通用 AI/教育创新比赛的证据状态，不给官方得分预测。
- 没有验证生产部署、多人并发、长期数据恢复、隐私合规或公众发布。

### 2.3 本轮生成的浏览器证据

- `output/playwright/competition-review-desktop.png`
- `output/playwright/competition-review-style-panel.png`
- `output/playwright/competition-review-after-save.png`
- `output/playwright/competition-review-mobile.png`

## 3. 当前最有竞争力的部分

1. **问题选择有辨识度。** 产品没有再做一个通用 AI IDE，而是把抽象 CSS 规则变成可操作的视觉实验。
2. **确定性画布链路已经成立。** 静态 HTML/CSS 导入、脚本剥离、沙箱 iframe、元素选择、样式检查、白名单修改、不可变版本与对比是实功能，不是演示稿。
3. **版本原则正确。** 正常单标签、未超本地配额的路径会创建新的不可变安全运行版本，而不是直接覆盖父版本；这为之后的教学证据与回放打下基础。并发旧版本保存、delete→undo 与配额失败仍未达到生产保证。
4. **AI 操作接口比直接操纵 DOM 更可信。** 动态工具参数有 schema、工具有白名单；当前按 `requestId` 的页面生命周期内 Map 提供有限幂等，失败可以返回给调用方。它还不是跨会话、带 payload digest 的持久幂等保证。
5. **安全与证据口径总体克制。** 上传脚本不执行；浏览器不接触 OAuth；日志清理认证材料与 SDP；静音机器延迟没有冒充真人听感。现有日志不等于已完成知情同意、PII 匿名化与保留/删除治理。
6. **术语已开始面向初学者。** “里面留白、外面留白、这个元素占了多大空间”等文案，比直接展示“盒模型”和内部阶段词更像产品。
7. **关键局部操作足够快。** 本机滑块会即时改变所选运行结果；保存后自动出现不可变前后对比。

这些不是需要推倒重来的原型债，而是值得保留的获奖底座。

## 4. 模拟真实用户后的结论

### 4.1 用户 A：第一次来、没有自己的文件

| 步骤 | 实际结果 | 评委/学生感受 | 严重度 |
|---|---|---|---|
| 打开产品 | 空画布，左侧先展示六种“块” | 不知道第一步要学什么，也看不到 90 秒示例 | 阻断 |
| 点击“知识卡” | 生成通用静态卡片 | 没法编辑，没有任务，没有下一步，形成死路 | 阻断 |
| 点击“实验页面” | 没有项目时只会得到空壳 | 学生理解不了为什么要先创建系统原语再上传 | 高 |
| 想文字问 AI | 文本框在语音会话建立前禁用 | 无麦克风、拒绝权限、听障或隐私用户无法学习 | 阻断 |

结论：首次体验是“画布编辑器的对象库”，不是“学生今天能完成的一节课”。

### 4.2 用户 B：载入示例并尝试学习

| 步骤 | 实际结果 | 评委/学生感受 | 严重度 |
|---|---|---|---|
| 导入示例 | 成功创建可运行实验 | 链路可靠，是当前最佳入口 | 优点 |
| 进入选择模式 | 需要先点“选择页面内容”再点 iframe | 可理解，但仍有两层模式成本 | 中 |
| 点击卡片文字 | 容易选到内部 `strong`/`div`，而非想改的卡片 | 不知道自己选错 DOM 层级 | 高 |
| 拖动“里面留白” | 数字、盒子尺寸与运行结果即时变化 | 操作反馈良好 | 优点 |
| 保存变化 | 产生 V2 与整页并排对比 | 版本可信，但局部变化在整页对比里不够突出 | 中 |
| 保存之后 | 没有提问、解释评分、迁移题或完成状态 | 产品无法判断学生是否理解 | 阻断 |

结论：学生能“把值调对”，系统不能证明学生知道“为什么”和“换个页面还会不会”。

### 4.3 用户 C：导入自己的真实页面

- 运行时能安全导入静态 HTML/CSS，这是很好的差异化入口。
- 但 AI 的 `read_canvas_state` 主要返回块、默认 selector、版本、控制器与对比摘要；没有读取当前选中元素、计算样式、命中规则及最小相关源码的完整教学事实工具。
- 因此“为什么我的页面没有居中”这类真实问题，AI 的诊断依据还不够。它更擅长创建固定 demo，而不是可靠理解用户页面。
- 当前没有代码编辑器。学生最终仍以拖滑块为主，不能在自己的 CSS 中独立复现同一效果。

结论：“上传自己的页面”目前是运行入口，还没有变成真正的个性化微课程。

### 4.4 用户 D：语音学习

- 已有日志证明三个主题可以通过工具创建 demo、控制器、版本与对比；这说明语音到画布的技术链成立。
- 当前 Realtime prompt 要求明确画布请求一次执行全部动作、首次工具成功前不追问、结束后只用一句话说明。这与规格中“先确认目标、追问判断、让学生操作、迁移”的教学顺序冲突。
- “静音”关闭麦克风轨道；“先停一下”只静音本地 AI 音频，并不等于取消模型响应或未完成工具动作。
- 设备本地最新日志记录过 `Connection reset without closing handshake`，会话以 error closed；没有看到浏览器侧自动恢复为可继续教学的证据。
- P95 `2331ms` 的终点是受控确认音开始播放，不是首个有意义的字幕或教学回答，比赛材料必须分开表述。

结论：当前是“语音能驱动画布”，还不是“轮次自然、可打断、可降级、可恢复的语音导师”。

### 4.5 用户 E：手机或小屏评委

- `390×844` 下顶部工具区高度为 142px，`.block-library` 使用 `overflow: hidden`。
- AI 面板实际位于该裁切区下方，语音与文字入口在视觉上不可达；上传入口也不具备可靠可达性。
- CSS 中存在大量 7–10px 字号，既影响投屏，也不符合成品级可读性。
- tldraw 仍显示 “Get a license for production” 水印。

结论：移动端不是降级体验，而是主入口被裁掉；比赛现场一旦换设备或投屏缩放，风险很高。

## 5. 与“第一名”之间的十个决定性差距

### 1. 没有可见、可测、可复核的学习闭环

规格写出了正确教学顺序，但领域模型、工具、UI 与测试没有把它变成状态机。当前只能证明“系统完成了操作”，不能证明“学生改变了错误心智模型”。

### 2. 首次一分钟没有主任务

没有一键示例、没有明确目标、没有完成标准，六种内部块类型把产品责任推给了初学者。评委最可能在看到核心价值前就迷路。

### 3. AI 自动执行逻辑与教学法相冲突

“一次完成全部画布动作”适合 Agent demo，却会绕过预测、等待、提示和学生解释。技术演示越顺，反而越容易被质疑“这是 AI 在做，学生学到了什么”。

### 4. AI 对真实用户页面的事实视野不足

缺少选中元素、相关 CSS 规则、源码片段、学生最后操作和教学证据等结构化读取能力。没有事实依据的个性化教学容易变成语言上的个性化。

### 5. 学生没有真正写 CSS

架构指定 CodeMirror，规格要求编辑、运行与重置，实际依赖和 UI 都没有代码编辑器。只拖滑块会制造“看懂了”的错觉，难以证明迁移能力。

### 6. Voice-first 缺少 text-first 降级、真正打断与恢复

文本依赖语音连接，移动端入口不可达，暂停不是取消，断连无自动续课。现场任何权限、网络或音频问题都会让核心叙事失效。

### 7. 产品化与可访问性尚未过线

静态占位块、英文内部版本摘要、DOM 标签名、整页小对比、微小字号、许可证水印和移动端裁切都在向评委暴露“这是工程验收界面”。

### 8. Realtime 的能力与 API 还没有形成可公开暴露的硬边界

Tutor 线程设置了 `read-only` 与 `approvalPolicy: never`，这是有效减损；但本机日志同时显示 `node_repl`、Spine、Blender、`codex_apps` 等无关 MCP 被启动。启动记录本身不能证明模型实际调用过这些能力，却足以说明当前没有“能力层只存在画布工具”的隔离证明。Live 路径必须用独立 profile/替代 provider 证明无关能力不可枚举、不可调用且无文件/网络副作用，不能把模型口头拒绝或 prompt 禁令当硬边界。

执行更新：Tutor 现使用独立最小 profile、临时 cwd 和服务端硬 allowlist；能力枚举与负向调用测试已通过。该结论只覆盖本机 loopback P6，不构成公网部署批准。

同时，当前 Realtime 的 session、input、tool result、events、log 与 diagnostics 路由没有完整 owner、Origin/CSRF 和限流边界。修复前服务只能绑定 loopback，不能声称可安全用于 LAN 或公网。

执行更新：上述路由已增加同 owner、Origin、字节/深度/键数、速率、TTL、幂等和资源回收边界；服务仍只绑定 `127.0.0.1`，未授权 LAN 或公网。

### 9. 交付可靠性与证据治理不足

- 根目录没有 `.git`，无法从当前交付物验证提交历史与可复现版本。
- 没有 `.github` CI。
- `AGENTS.md` 的 P6 授权漂移已于 2026-08-01 修正；自动防漂移检查尚未建立。
- `README.md` 的按钮文案等与实际 UI 漂移。
- 架构声明 CodeMirror、shadcn/ui 与一组 AI 工具，实际实现并不完整一致。

这些会削弱评委对“可交付、可维护、证据可信”的信心。

### 10. 没有真实学习成效与比赛叙事

现有测试覆盖安全、版本、工具、渲染和机器延迟；没有前后测、迁移测、提示依赖、真人任务完成率或专家评分。评委只能相信一段 demo，无法相信教育价值。

## 6. 通用竞赛证据状态（非官方评分）

在取得具体比赛评分表前，不计算总分，也不伪造“第一名门槛”。这里只标记现有证据成熟度：

| 维度 | 状态 | 判断 |
|---|:---:|---|
| 问题价值与差异化 | 绿 | 定位清楚，已有竞争力 |
| 技术底座与实现深度 | 绿 | 核心静态链路是实功能 |
| AI 事实 grounding 与可靠性 | 黄 | 能操作，但不够理解真实选中元素与源码 |
| 教学闭环与成效 | 红 | 只有操作链，没有可验证学习闭环 |
| 首次体验、视觉与可访问性 | 红 | 首屏、手机、字号和水印仍暴露原型感 |
| 证据与比赛叙事 | 红 | 缺形成性学生研究和迁移证据 |
| Live 安全、许可与现场韧性 | 红 | 能力隔离、API、断连、许可和 fallback 未过线 |

官方规则到位后再把这些证据映射到真实评分项。代码量和功能完成率不能替代学习结果、首次体验与证据。

## 7. 第一名必须先消灭的五个失冠点

1. **没有一键进入盒模型完整学习闭环。**
2. **AI 不能基于正确元素与真实 CSS 事实教学。**
3. **学生没有先预测、解释因果并完成陌生迁移。**
4. **拒绝麦克风或 Realtime 故障后，没有纯文字与确定性回退保证课程可完成。**
5. **没有即时学习证据收据与形成性真人研究，仍只能展示画布和机器测试。**

这五项任何一项存在，都不应该进入“冲第一名”的最终演示版本。若启用 Live 路径，硬能力隔离、owner/API 边界、许可与断线恢复是额外且不可豁免的条件性 no-go 门禁。

## 8. 候选 7 分钟体验（须先按官方时限重排）

这条路径只能在现场展示一个窄概念的、可追溯的单次陌生迁移证据，不能单独证明普遍教育有效性；教育效果只能引用预注册的真人研究。

1. **0:00–0:30**：学生点“一分钟学会为什么卡片会变大”，无需上传、登录或麦克风。
2. **0:30–1:10**：系统用一个问题诊断学生，把“里面空隙”和“外面空隙”区分开。
3. **1:10–2:00**：学生先预测 padding 变大后内容宽度与整体尺寸会怎样。
4. **2:00–3:00**：学生拖动并看到目标元素、公式、尺寸与局部前后镜头同步变化。
5. **3:00–4:00**：系统识别一个真实误区，只给一级提示；学生自己修正。
6. **4:00–5:30**：学生在 CSS 编辑器写出同一规则，并在结构不同的新卡片中完成迁移题。
7. **5:30–7:00**：展示当前会话的“原预测 → 操作 → 自我修正 → 单次迁移结果”学习证据收据；P7 最小实现现已支持跨刷新、可交互的 Learning Proof Replay，但须在用户批准后才可作为正式阶段成果。

这条体验同时回答了四个评委问题：为什么需要 AI、为什么需要画布、学生做了什么、如何形成可追溯的课内证据。它不替代教育有效性研究。

## 9. 建议的推进顺序

### Wave -1：先取得真实比赛约束

- 官方评分表、演示时长、联网/外部服务规则、数据与许可要求。
- 唯一首要画像、唯一舞台任务与可对外 claim。
- 在信息到位前，不把下述候选 Wave 当成官方排期。

### Wave 0：只做盒模型纵向获胜切片

- 一键盒模型课与明确主任务，30–45 秒内完成第一次拖动。
- 第一个二选一预测同时承担轻量诊断；AI 等待、追问、解释与陌生迁移形成闭环。
- 正确元素与真实 CSS 事实 grounding。
- 学生亲手补写一条 CSS 声明，并接受语义等价验证。
- 纯文字模式与明确标注的确定性 fallback。
- 5 人快速形成性走查后再决定是否进入演示硬化。

### Wave 1：硬化盒模型舞台路径

- 手机、字号、焦点、撤销/重做/单卡删除与独立全新演示空间。
- Live 路径的硬能力、owner/API、取消、重连、日志同意和许可门禁。
- 当前会话学习证据收据、预注册阈值与至少 12 人 pilot。
- 三次盲演和主备回退。

### Wave 2：盒模型通过后才扩展

- 盒模型达到冻结门槛后，再复制到 Flex 和定位。
- P7 最小实现已完成权威事件、受限学习者状态与 Learning Proof Replay；获用户批准后再决定是否扩展到 Flex、定位和通用画布时间线。
- 样本量由功效分析或置信区间目标决定的确认性成效研究。
- 生产发布、跨设备和公众合规只在对应门禁获批后推进。

Wave 只是优先级建议，不改变 `IMPLEMENTATION_PLAN.md` 的依赖顺序。P7 已批准，P8、部署、研究和后续阶段已统一授权；购买路径永久排除。缺少的真人、法务或官方证据仍必须如实记录。

## 10. 不应做的“伪创新”

- 再增加更多块类型，让画布看起来更忙。
- 加头像、3D 教室、表情动画，却没有学习闭环。
- 加更多模型、多 Agent 或模型切换器，把编排复杂度当创新。
- 让 AI 自动修完页面，再把它叫作教学。
- 用积分、徽章和烟花代替学习证据。
- 做炫目的 3D 盒模型，却不要求预测、解释和迁移。
- 过早扩展 React、Vue、全栈项目导入。
- 用 LLM 自评或 AI 生成的分数代替专家与真人证据。
- 把确认音 P95 说成 AI 有意义回答延迟。
- 做普通录屏回放，却不标出误区、自我修正和迁移。

## 11. 最终目标定义

“达到第一名准备度”不等于 100 项任务全部打勾，而是同时满足以下硬标准：

1. 新用户无需解释即可在 60 秒内进入第一项学生操作。
2. 5–7 分钟内形成一条有预测、操作、解释和单次陌生迁移的课内达成证据；这不等于长期掌握。
3. AI 的关键教学断言均能追溯到真实 DOM/CSS/学生事件证据。
4. 学生至少有一次亲手写 CSS，而不只是观看或拖滑块。
5. 拒绝麦克风、Realtime 断连和手机视口都有可继续完成课程的路径。
6. Tutor 进程级工具边界可以由测试和日志证明。
7. 至少 12 名目标学生 pilot 证明流程可用，并给出概念增益与迁移的方向性信号；教育效果 claim 需要另行预注册的比较研究和不确定性报告。
8. 比赛现场能展示真实的当前会话学习证据收据；盒模型 Learning Proof Replay 已有机器证据，但必须先获 P7 用户批准，并能说明每个数字的测量边界。
9. 所有面向评委的状态、文档、版本与演示包一致且可复现。
10. 正式设计、可访问性、许可证、隐私与故障恢复均无红线。

恰好 100 项、逐项可验收的目标清单见 `COMPETITION_FIRST_PLACE_GOALS.md`。
