# AI Tutor MVP 产品规格

## 1. 文档状态

- 状态：`APPROVED`
- 当前阶段：`P8_IN_PROGRESS / ALL_POST_P7_PHASES_AUTHORIZED / PURCHASES_EXCLUDED`；P6 与 P7 已于 2026-08-02 获用户明确批准，P8、部署、真人研究和后续阶段均已授权。禁止购买许可证、服务、设备、广告或参与者激励；缺少的真人、法务和比赛证据仍须如实标记。
- 实施前置条件：用户明确批准 `MVP_SPEC.md`、`ARCHITECTURE.md` 和 `IMPLEMENTATION_PLAN.md`。
- 产品依据：`deep-research-report.md` 中关于“前端新手调试、实时语音、共享教学画布、教学动作系统和可回放”的结论。

## 2. 产品定位

本产品是一个面向前端初学者的 Voice First CSS 可视化教学画布。

它不是通用 AI IDE，也不以自动完成整个项目为主要目标。

它的核心价值是把抽象的 HTML/CSS 规则变成可以拖动、对比、调节和回放的独立视觉实验，并通过实时语音引导学生理解变化原因。

## 3. MVP 目标

MVP 必须验证以下核心假设：

1. 学生能够通过语音问题或已有 HTML/CSS 文件进入教学流程。
2. AI 能把一个 CSS 问题拆成多个相互独立的可运行教学块。
3. 学生能通过前后对比、揭示滑块和 CSS 属性控制器直观看到样式变化。
4. 学生可以修改、复制和回退代码，而不是只能观看 AI 演示。
5. 系统能够记录学生、AI、代码和画布状态，最终完整回放教学过程。

## 4. 目标用户

MVP 优先服务以下用户：

- 正在学习 HTML/CSS 的零基础或弱基础学习者。
- 能看懂教程但无法独立定位布局问题的学习者。
- 已经写出静态页面，希望理解某个 CSS 规则为什么生效或不生效的学习者。

MVP 不优先服务资深工程师、复杂生产项目或全栈应用开发。

## 5. MVP 教学主题

第一版完整覆盖三个主题：

1. 盒模型：
   - `width`
   - `height`
   - `margin`
   - `border`
   - `padding`
   - `box-sizing`
2. Flex 布局：
   - `display`
   - `flex-direction`
   - `gap`
   - `justify-content`
   - `align-items`
3. CSS 定位：
   - `static`
   - `relative`
   - `absolute`
   - `top`
   - `right`
   - `bottom`
   - `left`

## 6. 用户入口

### 6.1 语音提问

用户可以直接通过语音提出 CSS 问题。

示例：

- “为什么这个卡片没有居中？”
- “padding 到底改变了什么？”
- “absolute 为什么会跑出原来的位置？”

AI 根据当前画布、选中元素和已有代码创建教学块并进行语音讲解。

P6 首版通过本机 Codex app-server 的实验性 Realtime 接口建立语音会话。
浏览器和应用代码不得读取、复制或持久化 Codex OAuth 凭据；身份验证和上游
ChatGPT/Codex Realtime 调用完全由 app-server 持有。该接入必须封装在可替换
Provider 后面，以便后续迁移到公开 OpenAI Realtime API。

语音回合在 VAD 检测到学生停止说话时先本地暂存 AI 输出，最终用户转写确认后再
区分概念问答与画布任务。画布任务在成功工具结果前不播放过程播报；首个画布操作
可见后立即显示学生可懂的确认，之后只在事实结果字幕出现时恢复模型输出。

P6 按用户选择保存设备本地结构化记录：同意保存学习内容时，记录最长保留 7 天，
可含转录、教学动作和连接指标；不同意时，只保存最长 24 小时且不含对话、页面或
工具内容的最小运行指标。两者都有 owner、容量上限、导出与删除，并清理原始音频、
SDP、OAuth 凭据、Cookie、Token、API Key 和已知 PII 模式。这些本机记录不是 P7
的权威数据库记录，也不能用于声称已经实现课程回放。

### 6.2 上传已有 HTML/CSS

用户可以拖放或选择以下文件：

- 一个 HTML 文件。
- 一个或多个 CSS 文件。
- HTML 中使用的本地图片。
- HTML/CSS 中使用的本地字体。

MVP 需要支持：

- HTML 内联 `<style>`。
- HTML 引用的本地 CSS。
- CSS 引用的本地图片和字体。
- 将导入文件保存为不可变的原始快照。
- 在隔离运行环境中还原页面。
- 让用户点击页面元素并以该元素作为讲解目标。

MVP 不执行上传文件中的 JavaScript。

如果文件中存在 JavaScript，系统可以展示源码和明确提示，但不能运行。

第一版不支持 ZIP 项目、React、Vue、Svelte、npm 依赖或构建脚本。

这些能力必须通过预留的运行时适配器接口在后续增加。

## 7. 核心用户流程

### 7.1 从语音问题开始

```text
进入空白或示例画布
→ 用户提出 CSS 问题
→ AI 判断教学意图
→ AI 创建原始状态教学块
→ AI 创建一个或多个对比状态
→ AI 添加代码差异、视觉标注或属性控制器
→ 学生拖动滑块、修改代码或复制实验分支
→ 系统重新渲染并评估结果
→ AI 根据学生操作继续追问或讲解
→ 保存并回放整个过程
```

### 7.2 从已有文件开始

```text
上传 HTML/CSS 和本地资源
→ 系统验证并建立 ImportSnapshot
→ 在沙箱中还原页面
→ 用户点击一个元素或通过语音描述问题
→ 系统读取相关 DOM、计算样式和命中规则
→ AI 提取最小相关代码
→ AI 创建独立教学块和可比较版本
→ 学生进行可视化实验
→ 系统保存版本、事件和讲解记录
```

## 8. 教学画布

画布必须支持：

- 无限平移和缩放。
- 拖动和调整教学块尺寸。
- 多选、分组和连接教学块。
- 聚焦 AI 当前讲解的教学块。
- 保存和恢复画布视口。
- 在存在较多教学块时暂停不可见块的实时运行。

画布中的教学块相互独立。

一个教学块的 CSS、DOM 或运行错误不能影响其他教学块或主应用。

## 9. 教学块类型

### 9.1 `explanation`

用于文字说明、步骤、问题和结论。

### 9.2 `runnable`

包含一组独立 HTML/CSS 文件、代码视图和渲染结果。

学生可以：

- 打开或隐藏代码。
- 编辑代码。
- 运行或重置。
- 复制为新实验分支。
- 选择渲染结果中的元素。

### 9.3 `comparison`

引用两个不可变代码版本并提供以下展示模式：

- 并排对比。
- 重叠揭示滑块。
- 代码差异视图。

### 9.4 `css-controller`

绑定一个教学块中的目标元素和 CSS 属性。

第一版支持：

- 带单位的数值滑块。
- 枚举值切换。
- 重置为原始值。
- 确认并保存为新代码版本。

拖动过程是临时实验状态。

只有用户确认修改或 AI 创建明确教学节点时才生成新代码版本。

### 9.5 `annotation`

用于箭头、高亮、盒模型覆盖层和视觉标注。

### 9.6 `group`

用于组织一次讲解中的多个相关教学块。

## 10. 混合教学模式

AI 可以：

- 创建演示块。
- 创建修改前后版本。
- 添加比较视图和 CSS 控制器。
- 高亮代码、DOM 元素或盒模型区域。
- 根据学生操作进行语音讲解。

学生可以：

- 修改任意可编辑教学块。
- 拖动 CSS 控制器。
- 复制教学块并建立自己的实验分支。
- 回退到任意历史版本。
- 请求更多提示或要求查看完整解释。

AI 不能悄悄覆盖学生或系统已有的代码版本。

## 11. 教学行为原则

AI 的默认教学顺序是：

```text
观察当前状态
→ 确认学习目标
→ 追问学生的判断
→ 创建最小可视化实验
→ 让学生操作
→ 对比预期与实际结果
→ 解释关键规则
→ 进行一次迁移练习或总结
```

AI 不应一开始就生成完整答案。

在用户明确要求直接演示时，AI 可以创建完整对比，但仍应解释关键变化并邀请学生操作。

## 12. 代码版本原则

所有重要代码修改都形成不可变版本。

版本必须记录：

- 父版本。
- 创建者：用户、AI 或系统。
- 文件快照。
- 修改摘要。
- 创建时间。
- 对应教学事件。

AI 修改、学生修改和导入原始文件必须保持清晰的来源边界。

滑块拖动的每个中间数值不生成版本，但需要进入可压缩的交互事件流。

## 13. 回放要求

回放必须能够按时间重现：

- 语音或文字对话。
- 教学块创建、移动、缩放、分组和聚焦。
- 代码版本创建和切换。
- CSS 控制器的关键变化。
- 页面渲染结果、错误和修复。
- AI 的教学动作。
- 学生的操作和最终结果。

回放不是录屏文件。

它是基于结构化事件和定期快照重建的可交互过程。

当前 P7 最小范围已经在盒模型纵向课程中实现：预测、保存实验版本、解释、
失败/成功迁移都作为版本化事件保存，可在刷新和数据库短暂不可用后恢复，并用
交互时间线重建到与最终快照一致的状态。完整 MVP 仍需把相同机制扩展到 Flex、
定位、语音时间点和通用画布操作，因此当前不能把盒模型机器验收写成三主题完整回放。

## 14. MVP 验收标准

### 14.1 功能验收

- 用户能够从语音问题进入教学流程。
- 用户能够上传静态 HTML/CSS 和本地图片、字体。
- 上传页面能够在隔离环境中正确还原。
- 用户能够点击渲染页面中的元素。
- 系统能够读取目标元素的计算样式、盒模型和命中 CSS 规则。
- AI 能够把局部问题拆成多个独立教学块。
- 画布支持平移、缩放、拖动和调整教学块尺寸。
- 至少实现 `explanation`、`runnable`、`comparison`、`css-controller` 和 `annotation`。
- 支持并排对比和揭示滑块对比。
- 支持代码修改、复制、回退和不可变版本。
- 刷新页面后能够恢复当前教学会话。
- 回放能够恢复画布、代码、关键滑块状态和语音时间点。

### 14.2 性能验收

- CSS 数值滑块到视觉更新的目标延迟低于 100ms。
- 简单 HTML/CSS 修改到重新渲染的目标延迟低于 500ms。
- 正常网络下，从用户停止说话到 AI 首次语音反馈的 P95 目标低于 2.5 秒。
- 画布存在 20 个教学块时仍可流畅操作。
- 不可见或远离视口的运行块暂停或降级为静态快照。

### 14.3 隔离与安全验收

- 任意教学块的 CSS 不得影响其他教学块。
- 上传文件中的 JavaScript 不得执行。
- 教学块不得直接访问主应用 DOM、存储或认证信息。
- 教学块默认不得发起外部网络请求。
- 所有跨 iframe 消息必须通过版本化协议和运行实例 ID 校验。
- 非法 AI 工具参数不得执行。
- AI 不得执行未注册的画布操作。
- 删除历史或清空画布必须得到用户确认。

### 14.4 教学场景验收

MVP 必须通过三条端到端场景：

1. 盒模型：
   - 导入一个卡片页面。
   - 选择卡片元素。
   - 展示 content、padding、border 和 margin。
   - 通过滑块调节 padding。
   - 创建修改前后对比和新代码版本。
2. Flex：
   - 从普通文档流生成 Flex 对比。
   - 调节 `gap`、`justify-content` 和 `align-items`。
   - 让学生解释主轴和交叉轴变化。
3. 定位：
   - 对比 `static`、`relative` 和 `absolute`。
   - 展示包含块和偏移参照。
   - 通过滑块改变偏移并保存实验版本。

## 15. MVP 明确不做

- 不做通用全栈 IDE。
- 不执行上传的 JavaScript。
- 不支持 React、Vue、Svelte 或 npm 项目导入。
- 不支持多人实时协作。
- 不提供完整课程商城、教师后台或机构管理。
- 不在第一版实现自动部署学生代码。
- 不允许 AI 直接生成并执行任意画布控制代码。

## 16. 后续能力接口

MVP 必须为以下能力保留接口，但不实现：

- Vanilla JavaScript 安全运行时。
- React/Sandpack 运行时。
- Vue、Svelte 等框架运行时。
- WebContainer/npm 项目运行时。
- 班级、教师和机构能力。
- 多人实时协作。
- 多模型提供商。
- 项目导出和部署。

## 17. 尚未确认但不阻塞文档评审的事项

- 具体视觉品牌、配色和字体。
- Codex app-server Realtime 的长期稳定性、配额和迁移到公开 API 后的成本策略。
- 生产部署平台和区域。
- 正式用户账户、订阅和机构权限设计。
- 中国大陆公众发布时的具体合规方案。
