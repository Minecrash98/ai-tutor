# P6 与盒模型纵向闭环机器验收

状态：`MACHINE_VERIFIED / USER_APPROVED_2026-08-02 / P6_COMPLETE`

最新机器证据更新：2026-08-02（`p6-performance-v2`）。

本文件只记录机器可复核结果。用户于 2026-08-02 明确批准 P6 并授权 P7 最小范围；该批准不等于真人听感、真人学习效果或比赛规则符合性。P8、部署、购买、真人招募和其他外部行为仍未授权。

## 验收条件

- 工作目录：仓库根目录
- 浏览器：Playwright Chromium；每个 Live 会话使用全新隔离上下文和设备本地存储。
- 未读取、清空或修改用户 Chrome 历史画布。
- 语音输入：Chromium 假麦克风 WAV；未访问物理麦克风。
- 语音输出：`--mute-audio`；因此音频事件是机器时序证据，不替代人工听感。
- 该目录当前不是 Git 仓库；不能声称已从干净 checkout 复现，也没有 commit/CI 来源证据。

## 盒模型纵向学习闭环

`tests/e2e/foundation.spec.ts` 的隔离浏览器路径通过：

1. 空存储、不登录、不上传、不授权麦克风，一次点击进入“一分钟盒模型课”。
2. 完成预测；错误预测不会直接揭晓答案。
3. 学生把 `padding` 从 `16px` 拖到 `32px` 并保存。
4. 错误解释得到一级提示，再完成因果解释。
5. 在 DOM 结构不同的新页面亲手输入一条 CSS；`margin: 20px` 被拒绝，`padding: 20px` 通过。
6. 收据展示预测、操作、解释次数和本次迁移，并明确“当前一节课的达成记录，不代表长期掌握”。

当前只验证一项即时迁移；第二项隐藏迁移和 24–72 小时延迟保持仍未实现，因此不得声称“长期掌握”。

## P6 三主题 Live 路径

| 主题 | 会话日志 | 真实结果 | HTTP / 日志 |
|---|---|---|---|
| 盒模型 | `ecaa1bc0-9692-4f88-ad93-f5bf8a81ae30` | demo、controller、40px 修改、comparison、学生拖到 44px、AI 重新读取并解释、停止与重连后画布不变 | 47 个请求全为 200；0 条 error 记录 |
| Flex | `f8289b8a-c36c-4b81-a9bc-e3af51d80df4` | demo、gap controller、修改、comparison | 34 个请求全为 200；0 条 error 记录 |
| 定位 | `29b47955-86a7-4e04-b266-e3824c8be0c1` | demo、top controller、修改、comparison | 31 个请求全为 200；0 条 error 记录 |

三个场景都实际完成 `create_demo_block`、`read_canvas_state`、`create_css_controller`、`apply_css_change` 和 `create_comparison`。盒模型的两次学生插话从服务端打断请求到新回合接受分别为 6ms 和 12ms；打断后旧回合工具调用为 0。输出门从 VAD 检测到学生停止说话时就先静音并暂存提前到达的 AI 字幕，等最终用户转写确认意图后才决定放行；画布任务继续抑制过程话术，在首个成功画布操作可见后显示“画布已经更新，你可以先看看。”，事实结果字幕出现时再恢复模型输出。

## P6 失败与恢复路径

- 纯文字 Live：2/2 通过；浏览器麦克风 API 调用为 0。
- 真正停止：活动回合被 `turn/interrupt` 中断，停止后无新字幕、音频或工具动作。
- 学生新问题：没有 pending 工具事务时打断旧朗读；pending 工具先完成事务边界。自动教学追问仍顺序排队。
- SSE 断线恢复：递增事件 ID 和 cursor 重放通过；恢复后事件单调且不重复工具副作用。
- 能力预检：在任何麦克风请求和 Realtime session 创建前完成；拒绝路径不创建音频轨道。
- 自适应追问：错误/不确定预测后，文字和假麦语音各只产生一次基于固定页面事实的追问。
- 日志 opt-out：只保留不含对话/页面内容的 24 小时运行记录；导出与删除通过，删除后 GET 为 404。
- API 边界：跨站、超大请求、owner、速率、TTL、幂等、日志和停止路径均有自动化覆盖。
- Live 运行结束后，本项目 `codex app-server` 残留进程数为 0。

## 延迟证据

正式 v2 批次为 10 个冷/暖配对、20/20 成功、0 失败，使用 nearest-rank。逐样本、识别文本、会话和回合 ID 见 `evidence/P6_LATENCY_SAMPLES_2026-08-02.json`。

| 指标 | n | 最小 | P50 | P95 | 最大 |
|---|---:|---:|---:|---:|---:|
| 最终用户转写 | 20 | 1621ms | 1874ms | 2072ms | 2139ms |
| 受控确认音 | 20 | 1632ms | 1889ms | 2088ms | 2148ms |
| 首个模型音频能量 | 20 | 1532ms | 1928ms | 2324ms | 2390ms |
| 画布可见 T1 | 20 | 6096ms | 7696ms | 10214ms | 10824ms |
| 首个有意义字幕 | 20 | 6143ms | 7733ms | 10228ms | 10859ms |
| 画布可见后到确认字幕 | 20 | 8ms | 17ms | 55ms | 55ms |

受控确认音合并 P95 为 2088ms，达到 P6 的 2500ms 首反馈目标；首模型音频能量 P95 为 2324ms，但画布任务中该远端音频会被本地输出门静音，不能冒充学生实际听到的结果回答。首个有意义字幕是成功画布操作可见后的产品确认，合并 P95 为 10228ms，且在画布可见后 P95 仅再等待 55ms。

v2 的 20 个回合全部在最终用户转写前截住了 1–7 个提前到达的 AI 字符。旧 `evidence/P6_LATENCY_SAMPLES_2026-08-01.json` 原样保留为 v1 历史证据；其字幕样本受“AI 输出可能早于最终用户转写约 20ms”竞态影响，不再用于当前延迟主张，也没有被删除或改写。

正式采样后又补跑一组带真实 DOM 断言的冷/暖会话
`14bf6594-7f57-4e21-a719-65e94e13dc97` /
`6b1c5acc-999a-4982-afef-c7ff22b9000e`，两轮都确认学生界面实际包含
“画布已经更新，你可以先看看。”。第一次补充运行曾因 `article` 同时包含可见
角色标签“AI”而被过严的全文相等断言判失败；原始会话保留，断言改为验证字幕内容
后冷/暖重跑通过。这是测试夹具误报，不从正式 20 样本中删除任何样本。

## 完整基线

2026-08-02 在当前最终代码上运行：

- `pnpm lint`：通过。
- `pnpm typecheck`：5 个项目通过。
- `pnpm test`：20 个测试文件，80/80 通过。
- `pnpm build`：Next.js 16.2.11 生产构建通过。
- `pnpm test:e2e`：21 个用例中 10 通过；11 个账户相关 Live 用例按默认开关跳过。
- 显式 Live：v2 延迟正式批次 10/10 冷暖配对通过；盒模型 1/1、Flex 与定位 2/2 的三主题路径此前通过；其余文字、停止、恢复、自适应、日志和合成语音用例均按上文独立运行并通过。
- 验证结束后本项目 `codex app-server` 残留进程为 0；本地页面返回 200，开发服务器 stderr 无新增内容。

## 尚需人类或新授权

- 真人语音听感与真实耳麦条件。
- P7 已授权最小范围，仍需实现和单独机器验收。
- P8 浏览器/设备/故障矩阵。
- 官方比赛规则、许可书面确认、真人 pilot、盲演、部署和 go/no-go 独立审查。
