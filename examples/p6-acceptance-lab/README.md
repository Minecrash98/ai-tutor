# CSS 空间与排列示例

这是一个可以直接载入学习画布的 HTML/CSS 示例，包含卡片空间、方块排列和标签位置三个主题。

## 加载方式

1. 打开 `http://127.0.0.1:3000`。
2. 点击左侧“载入我的页面”。
3. 同时选择本目录中的 `index.html` 和 `styles.css`。
4. 等待画布显示“页面已载入，可以开始调整”。

加载后可以点选三个区域中的内容，也可以通过文字或语音让 AI 创建调节滑块、修改样式和生成前后对比。

示例不包含 JavaScript、外部网络资源、字体或图片依赖。
