# AI Tutor MVP 实施计划

## 1. 当前状态

- 总状态：`P8_IN_PROGRESS`
- 2026-08-03 Demo v2：已生成并视觉验收 300.000 秒全英文 CSS 全局颜色变量主 MP4，包含真实认证文字 Tutor 的 2 次学生提问和 2 次 Tutor 回复；录制使用全新隔离浏览器、假媒体和 `--mute-audio`，物理麦克风调用 0。公开 YouTube URL 仍待上传后填写。
- 当前授权：P1–P5 已完成；P6 Realtime AI、盒模型纵向闭环和 P7 最小范围均于 2026-08-02 获用户明确批准；P8、部署、真人研究和后续阶段全部授权。
- 当前禁止：不得购买许可证、服务、设备、广告或参与者激励。没有真实参与者、专家、官方规则、书面许可或独立审查时，必须如实记录阻塞，不得伪造。
- P1 授权时间：2026-07-25。
- P2 授权时间：2026-07-25。
- P3 授权时间：2026-07-26。
- P4 授权时间：2026-07-26。
- P5 授权时间：2026-07-26。
- P6 授权时间：2026-07-26。
- P6 用户批准时间：2026-08-02。
- P7 最小范围授权时间：2026-08-02。
- P7 用户批准时间：2026-08-02。
- P8、部署、真人研究及后续阶段授权时间：2026-08-02。
- 购买排除确认时间：2026-08-02。

## 2. 已确认的实施原则

1. 使用全 TypeScript、Next.js、React 的模块化单体。
2. 使用 tldraw 构建无限教学画布。
3. 每个 HTML/CSS 教学块独立渲染并与其他块隔离。
4. AI 和学生都可以创建代码版本，但不能覆盖历史版本。
5. AI 只能通过预定义、可校验的结构化工具操作画布。
6. MVP 支持语音提问和上传已有 HTML/CSS 两种入口。
7. 第一版不执行上传文件中的 JavaScript。
8. 必须保留 `RuntimeAdapter`，以便后续支持 JavaScript、React、Vue 和 WebContainer。
9. 第一版教学范围是盒模型、Flex 和定位。
10. 实施先完成确定性的可视化链路，再接入 AI 和语音。

## 3. 阶段总览

```mermaid
flowchart LR
    P0["P0 文档批准"] --> P1["P1 项目基础"]
    P1 --> P2["P2 教学画布"]
    P2 --> P3["P3 导入与隔离运行"]
    P3 --> P4["P4 元素检查"]
    P4 --> P5["P5 滑块、对比和版本"]
    P5 --> P6["P6 Realtime AI"]
    P6 --> P7["P7 持久化与回放"]
    P7 --> P8["P8 三场景验收"]
```

所有阶段按顺序推进。

前一阶段的验收门没有通过时，不进入后一阶段。

## 4. P0：文档批准

### 目标

确保产品边界、架构、数据原则、协议、安全策略和验收条件已经明确。

### 交付物

- `MVP_SPEC.md`
- `ARCHITECTURE.md`
- `IMPLEMENTATION_PLAN.md`

### 验收门

- 用户确认三份文档可以作为实施依据。
- 所有用户提出的修改已同步到三份文档。
- 文档之间不存在技术栈、功能范围或阶段顺序冲突。

### 当前状态

`APPROVED`

## 5. P1：项目基础

### 目标

建立可持续开发、测试和扩展的单仓库基础。

### 任务

- 初始化 pnpm workspace。
- 创建 `apps/web` Next.js 应用。
- 创建 `packages/contracts`。
- 创建 `packages/runtime-core`。
- 创建 `packages/runtime-static-html`。
- 创建 `packages/teaching-model`。
- 配置 TypeScript 严格模式。
- 配置 Tailwind CSS 和基础设计变量。
- 配置 Vitest 和 Playwright。
- 配置 PostgreSQL、Drizzle 和初始 schema。
- 提供 `.env.example`，但不写入真实密钥。
- 添加格式、类型检查、测试和构建命令。

### 验收门

- 全新安装依赖后能够启动应用。
- 类型检查、单元测试和生产构建通过。
- Web 应用能显示产品专用的空画布入口。
- 数据库 schema 能生成并检查迁移。
- 不包含 Realtime、AI 或运行教学代码的临时实现。

### P1 验证记录

2026-07-25 完成以下验证：

- `pnpm lint`：通过。
- `pnpm typecheck`：5 个项目顺序通过。
- `pnpm test`：2 个测试文件、3 个测试通过。
- Drizzle：生成并检查 9 张表的初始迁移。
- Next.js：生产构建通过，根路由静态预渲染成功。
- 本地开发服务：根路由返回 HTTP 200。
- P2 画布、P3 运行时、Realtime AI、语音、回放和部署均未实施。

### 当前状态

`COMPLETED`

## 6. P2：教学画布

### 目标

建立可操作的无限画布和类型化教学块系统。

### 任务

- 集成 tldraw。
- 建立教学块与 tldraw Shape 的 ID 映射。
- 实现 `explanation` Shape。
- 实现 `runnable` Shape 外壳。
- 实现 `comparison` Shape 外壳。
- 实现 `css-controller` Shape 外壳。
- 实现 `annotation` 和 `group` 基础能力。
- 支持拖动、缩放、分组、连接和聚焦。
- 支持保存和恢复本地画布快照。
- 建立可见性检测，为运行时暂停做准备。

### 验收门

- 用户能在一个画布中创建和操作至少 20 个教学块。
- 块可以拖动、缩放、分组和连接。
- 自定义块内部按钮、编辑区域与画布拖动不会冲突。
- 刷新后可恢复本地画布状态。
- 画布不包含任何硬编码的静态 HTML 专属状态。

### 当前状态

`COMPLETED_AWAITING_P3_APPROVAL`

### P2 验证记录

2026-07-26 完成以下验证：

- `pnpm lint`：通过。
- `pnpm typecheck`：5 个项目顺序通过。
- `pnpm test`：4 个测试文件、8 个测试通过。
- `pnpm build`：生产构建通过，根路由静态预渲染成功。
- `pnpm test:e2e`：Chromium 真实浏览器验收通过。
- 浏览器验收覆盖：起始 6 块、块内按钮交互、一次生成 20 块、刷新后恢复 26 块。
- 本地开发服务：`http://localhost:3000` 返回 HTTP 200。
- P3 iframe 运行时、文件执行、Realtime AI、语音、回放和部署均未实施。

## 7. P3：导入与隔离运行

### 目标

完成静态 HTML/CSS 文件导入和独立教学块渲染。

### 任务

- 定义并测试 `RuntimeAdapter`。
- 实现 `StaticHtmlCssAdapter`。
- 实现多文件选择和拖放。
- 验证 HTML、CSS、图片和字体。
- 建立不可变 `ImportSnapshot`。
- 解析 HTML 的本地 CSS 引用。
- 解析 CSS 中的本地资源引用。
- 将本地资源转换为受控 URL。
- 创建沙箱化 iframe。
- 注入最小运行时桥接代码。
- 实现 iframe 版本化消息协议。
- 阻止导入 JavaScript 执行。
- 阻止外部网络和顶层导航。
- 实现运行时挂载、暂停、恢复和销毁。

### 验收门

- 能导入一个 HTML 文件和多个 CSS 文件。
- 内联样式和本地 CSS 能正确生效。
- 本地图片和字体能正确显示。
- 上传文件中的 JavaScript 不执行。
- 不同教学块中的全局选择器互不影响。
- 运行时错误能够返回主应用并明确展示。
- 不可见运行块能够暂停或切换为快照。

### 当前状态

`COMPLETED`

## 8. P4：元素选择与样式检查

### 目标

让用户能够从上传页面直接选择问题元素，并取得可教学的数据。

### 任务

- 实现教学块内的元素选择模式。
- 绘制 hover 和 selected 覆盖层。
- 生成稳定的 `ElementTarget`。
- 读取 bounding rect。
- 读取盒模型尺寸。
- 读取计算样式。
- 解析与目标元素相关的 CSS 规则。
- 显示选择器、来源文件、行列位置和优先级信息。
- 为无匹配、伪元素和继承样式提供明确诊断。

### 验收门

- 用户可以点击教学块内任意可见元素。
- 主应用能显示目标元素的盒模型和关键计算样式。
- 系统能指出主要命中规则及其来源。
- 重新渲染后能重新定位目标，无法定位时给出明确提示。
- 检查器不能突破 iframe 安全边界。

### 当前状态

`COMPLETED`

### P4 验证记录

2026-07-26 完成以下验证：

- `pnpm lint`：通过。
- `pnpm typecheck`：5 个项目顺序通过。
- `pnpm test`：5 个测试文件、14 个测试通过。
- `pnpm build`：生产构建通过，根路由静态预渲染成功。
- `pnpm test:e2e`：3 个 Chromium 真实浏览器场景通过。
- 浏览器验收覆盖：hover 与 selected 覆盖层、稳定 DOM target、盒模型、关键计算样式、CSS 来源与行列、优先级、继承、伪元素、无直接命中和重新定位失败诊断。
- iframe 仍只启用 `allow-scripts`，未启用 `allow-same-origin`；检查结果继续经过来源窗口、运行实例 ID、协议版本和 Zod schema 校验。
- P5 CSS 滑块、临时样式、比较和代码版本均未实施。

## 9. P5：CSS 控制、对比与版本

### 目标

完成最核心的“看见 CSS 改变了什么”体验。

### 任务

- 实现数值加单位的 CSS 滑块。
- 实现枚举值控制器。
- 实现临时样式应用和重置。
- 实现“保存实验”为新代码版本。
- 实现父子版本关系。
- 实现版本复制、切换和回退。
- 实现并排对比。
- 实现重叠揭示滑块。
- 实现代码差异视图。
- 实现盒模型视觉覆盖层。
- 实现旧版本和新版本同步定位同一元素。
- 将 P4 检查器默认收敛为教学视图，完整规则和诊断保留在高级视图。

### 验收门

- 滑块改变后视觉反馈目标低于 100ms。
- 保存实验后产生新版本，原版本保持不变。
- 可在任意两个版本之间建立比较。
- 并排和揭示滑块能正确展示相同尺寸的两个结果。
- 刷新后版本关系和比较配置仍可恢复。
- 盒模型场景无需 AI 即可完整演示。

### 当前状态

`COMPLETED_AWAITING_P6_APPROVAL`

### P5 验证记录

2026-07-26 完成以下验证：

- `pnpm lint`：通过。
- `pnpm typecheck`：5 个项目顺序通过。
- `pnpm test`：6 个测试文件、18 个测试通过。
- `pnpm build`：Next.js 生产构建通过，根路由静态预渲染成功。
- `pnpm test:e2e`：4 个 Chromium 真实浏览器场景通过。
- 浏览器验收覆盖：默认精简教学视图、高级规则切换、盒模型覆盖层、白名单 CSS 滑块、低于 100ms 的本机交互目标、重置、不可变父子版本、历史切换与回退、复制分支、双沙箱目标同步、并排/揭示/代码差异，以及刷新后恢复版本关系和比较配置。
- P5 刷新恢复采用设备本地存储；PostgreSQL 权威持久化和结构化回放仍保留在 P7，未提前实施。
- 在 P5 验收当时，P6 Realtime AI、语音、回放和部署均未实施；当前 P6 状态与证据见下一节，P7 回放和部署仍未实施。

## 10. P6：Realtime AI 教学

### 目标

让 AI 能通过语音理解问题，并用结构化动作构建和控制教学画布。

### 任务

- 定义可替换的 `RealtimeProvider`，首版实现本机 Codex app-server provider。
- 通过 stdio JSON-RPC 初始化 app-server，不读取或复制 Codex OAuth 凭据。
- 实现服务端 Realtime 会话、事件、工具结果和停止接口。
- 建立浏览器 WebRTC 音频连接。
- 建立 Realtime DataChannel 事件处理。
- 实现连接、静音、打断、重连和文字转录状态。
- 定义 Zod 工具 schema。
- 实现工具注册表和执行器。
- 实现工具权限、实体存在性和幂等校验。
- 将画布、块、元素检查和版本摘要提供给教学编排器。
- 实现基础教学策略：
  - 追问。
  - 最小可视化实验。
  - 学生操作等待。
  - 结果验证。
  - 总结。
- 记录工具调用、执行结果和语音时间点。
- 将 P6 诊断事件按会话追加到设备本地 NDJSON 文件。
- 记录麦克风电平、WebRTC 连接状态和出站音频统计，不记录原始音频。
- 提供脱敏日志下载和课程概要素材导出；不提前实现 P7 数据库与回放。

### P6 已确认接入决策

- 浏览器调用 Next.js `/api/realtime/*`，不直接访问 ChatGPT/Codex 上游。
- Next.js 服务端通过本机 `codex app-server` 的
  `thread/realtime/start` 建立 V3 WebRTC 会话。
- 首版默认模型为 `gpt-live-1-codex`，但模型名只存在于 provider 配置层。
- app-server 的 `dynamicTools` 和 `item/tool/call` 是画布操作的唯一 AI 入口。
- Codex OAuth 由 app-server 自己管理；应用不读取 `auth.json`，也不把 Token
  返回浏览器。
- app-server Realtime 属于实验接口，因此所有调用必须有稳定错误码、能力探测
  和未来切换公开 OpenAI Realtime API 的替换边界。
- P6 本机日志用于诊断和课程概要素材，采用追加写入并删除认证材料和原始 SDP；
  PostgreSQL 权威持久化、快照和回放继续由 P7 实现。

### 当前状态

`COMPLETED / USER_APPROVED_2026-08-02`

### 验收门

- 用户可以通过语音开始三个 CSS 主题之一的教学。
- AI 只使用已注册工具改变画布。
- 非法或未知工具调用不会执行。
- AI 能创建原始块、修改版本、比较块和 CSS 控制器。
- AI 能在学生操作后读取结果并继续讲解。
- 正常网络下首语音反馈 P95 目标低于 2.5 秒。
- AI 失败时保留现有画布和代码，不造成数据损坏。

### P6 机器验证记录

2026-08-01 至 2026-08-02 完成以下机器验证；完整可复核记录见
`P6_MACHINE_ACCEPTANCE_2026-08-01.md`。用户于 2026-08-02 明确批准 P6：

- 所有浏览器测试使用全新 Playwright Chromium 上下文和隔离设备存储，未读取、
  清空或修改用户 Chrome 历史画布；语音使用 Chromium 假麦克风并加
  `--mute-audio`，未访问物理麦克风。
- 盒模型、Flex、定位三个独立 Live 场景 3/3 通过。每个场景都实际执行
  `create_demo_block`、`read_canvas_state`、`create_css_controller`、
  `apply_css_change` 和 `create_comparison`。
- 盒模型学生把控制器从 `40px` 拖到 `44px` 后，AI 重新读取画布并正确解释；
  两次学生插话到新回合接受为 6ms、12ms，打断后旧回合工具调用为 0；停止和重连
  前后语义状态一致。
- 语音输出门从 VAD 语音结束时就静音并暂存提前 AI 输出，等最终用户转写分类后再
  放行；画布任务抑制过程播报，在首个成功画布操作可见后立即显示学生可懂的确认，
  再在事实结果字幕出现时恢复模型输出。纯文字 Live 不调用麦克风；活动文字回合
  停止后无后续字幕或工具动作。
- SSE 使用递增事件 ID 与 resume cursor；真实断线恢复验证事件单调、无重复工具
  副作用。能力预检发生在麦克风请求和 session 创建前。
- 错误/不确定预测会触发一次固定事实自适应追问；文字和假麦语音各 1/1 通过。
- 工具 allowlist、最小临时 cwd、owner/Origin/请求边界、TTL、速率、幂等、结构化
  学生文本和进程回收均有自动化证据；Live 结束后本项目 app-server 残留进程为 0。
- 日志已拆分为 7 天含内容学习记录与 24 小时无内容运行记录；两者均有上限、
  owner、导出/删除和脱敏策略。opt-out Live 删除后 GET 返回 404。
- 2026-08-02 正式 v2 延迟批次为 10 个冷/暖配对、20/20 成功、0 失败，
  nearest-rank 合并结果：最终用户转写 P95 `2072ms`、受控确认音 P95
  `2088ms`、首模型音频能量 P95 `2324ms`、画布可见 T1 P95
  `10214ms`、首有意义字幕 P95 `10228ms`；字幕在画布可见后 P95 再等待
  `55ms`。20/20 回合均截住 1–7 个最终用户转写前的提前 AI 字符。远端模型
  音频在画布工具阶段被静音，不能冒充学生实际听到的结果回答。逐样本见
  `evidence/P6_LATENCY_SAMPLES_2026-08-02.json`；v1 JSON 原样保留为受已修复
  输出竞态影响的历史证据，不再用于当前主张。
- `pnpm lint`：通过。
- `pnpm typecheck`：6 个工作区项目通过。
- 最终候选必须重新运行 `pnpm test`；测试数量由结构化门禁结果记录，不在计划中硬编码。
- `pnpm build`：当前 Next.js 16.2.12 生产构建通过。
- 当前 P8 完整基线：单元/集成测试 239 项通过、10 个账户条件场景跳过；E2E
  67 项通过、18 个账户 Live 场景跳过、0 失败。显式 P6 Live 与延迟用例继续由各自冻结证据
  单独报告，不能用默认跳过结果冒充重复 Live 验收。
- 以上机器证据不替代真人听感；用户批准的是 P6 阶段门禁，不是对真人听感或学习
  效果的声明。P7 最小范围随后获授权；P8、部署、真人研究与后续阶段现已统一授权，
  购买永久排除。

## 11. P7：持久化与回放

### 当前状态

`COMPLETED / USER_APPROVED_2026-08-02`

P7 获批时的最小范围只包括：

- 版本化学习证据事件 schema。
- 设备当前运行环境中的权威会话持久化、幂等和恢复。
- 画布、语义状态与素材的事务一致性，以及配额、schema 迁移和损坏救援。
- 可交互的 Learning Proof Replay，展示预测、操作、解释、提示、自我修正、
  CSS 迁移与缺失/失败证据。

账号、多用户、生产部署、长期合规保留策略和 P8 浏览器矩阵不属于当时的 P7 最小
验收；后续 P8 与发布工作已经另行统一授权并执行。

### 目标

让教学会话可恢复、可审计和可重放。

### 任务

- 实现 PostgreSQL 和 Drizzle 持久化。
- 保存画布、教学块、导入快照和代码版本。
- 实现严格递增的会话事件序号。
- 批量保存高频事件。
- 对滑块事件进行合并或采样。
- 定期生成会话快照。
- 实现回放读取接口。
- 实现回放时间线。
- 恢复块创建、位置、尺寸、版本和关键滑块变化。
- 对齐语音转录时间点和教学动作。
- 实现刷新恢复和异常中断恢复。

### 验收门

- 刷新页面后可以继续当前会话。
- 回放可以从开始重建到最终画布状态。
- 回放结果与最终快照一致。
- 重复提交事件不会产生重复版本或重复教学块。
- 导入原始快照和代码历史不可修改。
- 数据库暂时不可用时不会丢失当前浏览器中的正在编辑状态。

### P7 最小范围机器验证记录

2026-08-02 已完成当前授权最小范围：

- 共享合约定义版本化盒模型事件、确定性评价证据、会话、快照和回放 bundle；
  所有事件带 UUID、时间、actor 与版本，实验事件带不可变 revision ID。
- PostgreSQL/Drizzle 已保存 owner-bound 会话、严格递增事件、payload hash、
  确定性快照和最终画布/语义状态；重复请求幂等，同 ID 异参和过期序号返回冲突。
- 浏览器带校验和的本地 outbox 支持刷新恢复、旧 schema 迁移、损坏指针、配额错误
  和权威事件合并；数据库不可用时不清空当前学习记录。
- 盒模型 Learning Proof Replay 可拖动、前后步进和自动播放，保留错误预测、错误解释、
  迁移尝试与最终结果；最终 reducer 状态必须与权威快照相等。
- 专用 PostgreSQL 16 测试库迁移与 2/2 集成测试通过；完整 lint、类型、单元、构建与
  启用数据库的 E2E 基线通过。隔离真实浏览器还完成错误解释、错误迁移、正确迁移、
  回放和刷新恢复，未访问物理麦克风或 Chrome 历史画布。
- 详细命令、数据库计数、截图和诚实边界见 `P7_MACHINE_ACCEPTANCE_2026-08-02.md`。

这只记录当时的盒模型 P7 最小范围，不等于真人学习效果或生产合规。
后续 P8 已补齐 Flex/定位，以及 Tutor 文字/合成语音、工具、事实 receipt 与画布保存动作
的统一时间线；机器验收见
`docs/P8_LEARNING_PROOF_REPLAY_ACCEPTANCE_2026-08-03.md`。多设备同步和生产保留策略
仍不在这项机器结论内。

## 12. P8：三场景验收

### 当前状态

`LOCAL_MACHINE_CANDIDATE_VERIFIED / GOAL_LEDGER_75_VERIFIED_1_IN_PROGRESS_24_BLOCKED / EXTERNAL_GATES_BLOCKED`

P8、部署、真人研究和后续阶段已统一授权；购买永久排除。执行仍按依赖顺序推进，
机器测试、真人验收、官方规则、许可证和独立审查分别记录，互不冒充。

当前本地候选已走通盒模型、Flex、定位从入口到权威回放，并完成浏览器/移动端、
故障、安全、视觉、自动无障碍、性能、发布、SBOM、漏洞扫描和隔离备份恢复矩阵。
最终无缓存 Compose 候选核心浏览器套件 25/25 通过；100 项已无 `NOT_STARTED`。
完整命令结果、发布指纹、语音与性能数值及未发生事项见
`docs/RESULTS_AND_LIMITATIONS_2026-08-02.md`。P8 不能整体宣布完成，因为正式比赛
规则、真人/专家/法务、Git 来源、第二设备与网络、盲演和独立 go/no-go 仍没有外部证据；
目标 083 的 30 分钟浸泡按用户要求不再重跑并保持未验证；目标 094 的通用
Tutor 文字/合成语音、工具、事实与画布统一 replay 已通过机器验收。提交与视频只完成
流程设计，尚未制作或提交，见 `docs/SUBMISSION_AND_VIDEO_DEMO_PLAN.md`。

### 目标

用真实端到端教学任务验证产品闭环。

### 场景 A：盒模型

```text
上传卡片页面
→ 选择卡片
→ AI 展示盒模型
→ 学生拖动 padding
→ 保存实验版本
→ 创建前后对比
→ AI 总结 box-sizing 的影响
→ 回放
```

### 场景 B：Flex

```text
导入普通文档流
→ AI 创建 Flex 分支
→ 并排比较
→ 学生调节 gap、主轴和交叉轴
→ AI 检查学生理解
→ 保存并回放
```

### 场景 C：定位

```text
创建 static、relative、absolute 三个独立块
→ 标注包含块
→ 学生调节偏移
→ AI 解释脱离文档流
→ 比较代码和视觉结果
→ 保存并回放
```

### 最终验收门

- `MVP_SPEC.md` 中的功能、性能、安全和教学验收全部通过。
- 三条场景均能从入口运行到回放。
- 类型检查、单元测试、集成测试和生产构建通过。
- 没有执行上传 JavaScript。
- 没有发现跨教学块 CSS 污染。
- 没有发现 AI 绕过工具协议操作画布。

## 13. 测试策略

### 单元测试

- 运行时适配器能力匹配。
- 导入引用解析。
- HTML 脚本剥离或阻止。
- CSS 控制值序列化。
- 代码版本父子关系。
- Zod 工具 schema。
- 事件排序和幂等。

### 集成测试

- 主应用与 iframe 消息协议。
- 元素选择和计算样式读取。
- 临时样式到代码版本的提交。
- 比较块与两个运行时同步。
- Realtime 工具调用到画布动作。
- 数据库事件和快照恢复。

### 浏览器端到端测试

- 上传 HTML/CSS。
- 拖动和缩放教学块。
- 选择元素。
- 拖动 CSS 滑块。
- 保存新版本。
- 创建前后对比。
- 刷新恢复。
- 回放。

### 安全测试

- 上传脚本不会执行。
- `javascript:`、外部导航和表单提交被阻止。
- iframe 不能访问主应用。
- 非法 `postMessage` 被忽略。
- 非法 AI 工具参数被拒绝。

## 14. 风险与应对

| 风险 | 影响 | 当前应对 |
|---|---|---|
| 画布内大量 iframe 导致性能下降 | 拖动和缩放卡顿 | 可见区挂载、远处快照、暂停运行时 |
| HTML/CSS 资源引用复杂 | 导入结果与原页面不同 | 规范化资源清单、明确诊断、限制 MVP 输入 |
| CSS 规则来源难以准确解释 | 教学结论不可信 | 浏览器计算样式加解析器规则来源，区分事实与推断 |
| 揭示滑块两侧布局尺寸不一致 | 对比错位 | 固定比较视口、同步尺寸、显示差异提示 |
| AI 创建过多教学块 | 画布失控 | 工具限制、教学步骤预算、分组和聚焦 |
| Realtime 网络抖动 | 语音体验下降 | 状态提示、打断、重连、文本降级 |
| 事件量过大 | 存储和回放变慢 | 高频事件合并、定期快照 |
| 后续 React 运行时迫使核心重写 | 扩展成本高 | 从第一天使用 `RuntimeAdapter` 和能力声明 |

## 15. 实施过程守则

- 每个阶段开始前更新本文档状态。
- 每个阶段只修改该阶段授权的范围。
- 阶段验收失败时先修复，不提前铺设后续功能。
- 不把临时演示代码当作正式运行时边界。
- 不用假数据声称 Realtime、回放或数据库已经可用。
- 不以构建通过替代产品验收。
- 不在未验证时声称 CSS 隔离、性能或安全目标已达到。
- 新增范围必须先同步 `MVP_SPEC.md` 和 `ARCHITECTURE.md`。

## 16. P0 批准后的第一个执行批次

只有在用户批准计划后，才执行以下批次：

1. 初始化 pnpm workspace。
2. 创建 Next.js Web 应用和共享 packages。
3. 配置严格 TypeScript、测试和构建。
4. 创建空白 tldraw 画布。
5. 创建第一个可拖动的 `runnable` 教学块外壳。
6. 运行类型检查、测试和构建。
7. 汇报真实结果后等待进入 P3。

该批次不会接入 AI、语音、文件执行或生产部署。
